<div class="container">
  <% if @user.id == current_user.id %>
    <h2 class="favo-title">フォローリスト</h2>
  <% else %>
    <h2><%= @user.name %>さんのフォローリスト</h2>
  <% end %>
  <% @users.each do |follower| %>
    <div class="fovo-container">
      <div class="favo-image">
        <% if follower.image.attached? %>
          <%= image_tag follower.image %>
        <% else %>
          <%= image_tag "20200501_noimage.jpg" %>
        <% end %>
        <ul class="favo-list">
          <li class="favo-pet-name"><%= follower.name %></li>
        </ul>
      </div>
      <%= link_to "詳細", user_path(follower.id), class: "detail-btn" %>
      <% if current_user.id != @user.id %>
        <% if current_user.following?(@user) %>
          <span class="followed">フォロー済み</span>
        <% else %>
          <%= link_to 'フォローする', user_relationships_path(@user.id), method: :POST, class: "list-follow-btn" %>
        <% end %>
      <% end %>
      <% if current_user.id == @user.id %>
        <% if current_user.following?(@user) %>
        <% else %>
          <%= link_to 'フォローを外す', user_relationships_path(@user.id), method: :delete, class: "list-unfollow-btn" %>
        <% end %>
      <% end %>
    </div>
  <% end %>
</div>